<div class="bg-white p-6 rounded-xl shadow-md max-w-lg mx-auto">
    <h2 class="text-xl font-semibold mb-6 border-b pb-4"><?= e($title); ?></h2>
    <form action="<?= APP_URL; ?>/constants/updateSite" method="POST">
        <input type="hidden" name="id" value="<?= e($site->id); ?>">
        <div class="space-y-4">
            <div>
                <label for="site_name" class="block text-sm font-medium text-gray-700 mb-1">نام سایت</label>
                <input type="text" name="site_name" id="site_name" value="<?= e($site->site_name); ?>" required class="w-full px-4 py-2 border border-gray-300 rounded-lg">
            </div>
            <div>
                <label for="site_url" class="block text-sm font-medium text-gray-700 mb-1">آدرس سایت</label>
                <input type="url" name="site_url" id="site_url" value="<?= e($site->site_url); ?>" required class="w-full px-4 py-2 border border-gray-300 rounded-lg" dir="ltr">
            </div>
        </div>
        <div class="mt-8 pt-5 border-t flex justify-end">
            <a href="<?= APP_URL; ?>/constants/sites" class="bg-gray-200 text-gray-700 font-bold py-2 px-6 rounded-lg ml-4">انصراف</a>
            <button type="submit" class="bg-blue-600 text-white font-bold py-2 px-6 rounded-lg">به‌روزرسانی</button>
        </div>
    </form>
</div>
