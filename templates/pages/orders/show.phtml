<div class="bg-white p-6 rounded-xl shadow-md max-w-4xl mx-auto">
    <div class="flex justify-between items-center mb-6 border-b pb-4">
        <h2 class="text-xl font-semibold"><?= e($title); ?></h2>
        <a href="<?= APP_URL; ?>/orders" class="bg-gray-200 text-gray-700 font-bold py-2 px-4 rounded-lg hover:bg-gray-300">بازگشت به لیست</a>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <!-- ستون سمت راست: تصویر و اطلاعات اصلی -->
        <div class="md:col-span-1">
            <?php if (!empty($order->image)): ?>
                <img src="<?= APP_URL . '/uploads/orders/' . e($order->image); ?>" alt="<?= e($order->title); ?>" class="w-full h-auto object-cover rounded-lg shadow-md">
            <?php else: ?>
                <div class="w-full h-48 bg-gray-200 rounded-lg flex items-center justify-center text-gray-500">بدون تصویر</div>
            <?php endif; ?>
            <div class="mt-4 text-center">
                <span class="bg-blue-500 text-white text-sm font-medium px-4 py-1 rounded-full">
                    <?= e($order->status_name); ?>
                </span>
            </div>
        </div>

        <!-- ستون سمت چپ: جزئیات کامل -->
        <div class="md:col-span-2">
            <h3 class="text-lg font-semibold text-gray-800 mb-4"><?= e($order->title); ?></h3>
            <div class="space-y-3 text-sm">
                <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">مشتری:</span>
                    <span class="font-medium text-gray-800"><?= e($order->customer_name); ?></span>
                </div>
                <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">قیمت محصول (تومان):</span>
                    <span class="font-medium text-green-600"><?= number_format($order->price_toman ?? 0); ?> تومان</span>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                    <div class="flex justify-between border-b pb-2">
                        <span class="text-gray-500">هزینه باربری:</span>
                        <span class="font-medium text-gray-800"><?= number_format($order->shipping_cost_toman ?? 0); ?> تومان</span>
                    </div>
                    <div class="flex justify-between border-b pb-2">
                        <span class="text-gray-500">هزینه کارگو:</span>
                        <span class="font-medium text-gray-800"><?= number_format($order->cargo_cost_toman ?? 0); ?> تومان</span>
                    </div>
                    <div class="flex justify-between border-b pb-2">
                        <span class="text-gray-500">هزینه خرید استور:</span>
                        <span class="font-medium text-gray-800"><?= number_format($order->store_cost_toman ?? 0); ?> تومان</span>
                    </div>
                </div>
                <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">جمع کل:</span>
                    <span class="font-bold text-green-700"><?= number_format($order->total_cost ?? ($order->price_toman ?? 0)); ?> تومان</span>
                </div>
                <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">قیمت (لیر):</span>
                    <span class="font-medium text-gray-800"><?= number_format($order->price_lira ?? 0, 2); ?> لیر</span>
                </div>
                <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">کد رهگیری:</span>
                    <span class="font-mono text-gray-800"><?= e($order->tracking_code); ?></span>
                </div>
                <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">تعداد / سایز / وزن:</span>
                    <span class="font-medium text-gray-800"><?= e($order->quantity); ?> عدد / <?= e($order->size); ?> / <?= e($order->weight); ?> kg</span>
                </div>
                <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">دسته‌بندی:</span>
                    <span class="font-medium text-gray-800"><?= e($order->category_name ?? 'ندارد'); ?></span>
                </div>
                <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">سایت مبدا:</span>
                    <?php if(!empty($order->source_site)): ?>
                        <a href="<?= e($order->source_site); ?>" target="_blank" class="font-medium text-blue-600 hover:underline">مشاهده لینک</a>
                    <?php else: ?>
                        <span class="font-medium text-gray-800">ثبت نشده</span>
                    <?php endif; ?>
                </div>
                <!-- FIX: Added Product Link display -->
                <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">لینک محصول:</span>
                     <?php if(!empty($order->product_link)): ?>
                        <a href="<?= e($order->product_link); ?>" target="_blank" class="font-medium text-blue-600 hover:underline">مشاهده لینک محصول</a>
                    <?php else: ?>
                        <span class="font-medium text-gray-800">ثبت نشده</span>
                    <?php endif; ?>
                </div>
                 <div class="flex justify-between border-b pb-2">
                    <span class="text-gray-500">بارکد:</span>
                    <a href="javascript:void(0);" onclick="showBarcode('<?= e($order->barcode_url); ?>')" class="font-medium text-blue-600 hover:underline">مشاهده بارکد</a>
                </div>
                <div class="flex justify-between border-b pb-2 text-xs">
                    <span class="text-gray-500">ویژگی‌ها:</span>
                    <div class="flex space-x-2 space-x-reverse">
                        <span>هزینه باربری: <?= $order->apply_shipping ? '<span class="text-green-600 font-semibold">بله</span>' : 'خیر'; ?></span>
                        <span>با کارگو: <?= $order->with_cargo ? '<span class="text-green-600 font-semibold">بله</span>' : 'خیر'; ?></span>
                        <span>خرید استور: <?= $order->is_store_purchase ? '<span class="text-green-600 font-semibold">بله</span>' : 'خیر'; ?></span>
                    </div>
                </div>
                <div class="pt-2">
                    <p class="text-gray-500 text-xs mb-1">توضیحات:</p>
                    <p class="text-gray-700 text-justify"><?= e($order->description ?? 'توضیحات ثبت نشده است.'); ?></p>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Barcode Modal -->
<div id="barcodeModal" class="fixed inset-0 bg-black bg-opacity-50 hidden items-center justify-center z-50">
    <div class="bg-white p-4 rounded-lg shadow-lg text-center">
        <img id="barcodeImage" src="" alt="Barcode" class="mx-auto">
        <button onclick="closeBarcodeModal()" class="mt-4 bg-red-500 text-white px-4 py-2 rounded-lg">بستن</button>
    </div>
</div>

<script>
    function showBarcode(url) {
        if (!url) return;
        document.getElementById('barcodeImage').src = url;
        document.getElementById('barcodeModal').classList.remove('hidden');
        document.getElementById('barcodeModal').classList.add('flex');
    }
    function closeBarcodeModal() {
        document.getElementById('barcodeModal').classList.add('hidden');
        document.getElementById('barcodeModal').classList.remove('flex');
    }
</script>
